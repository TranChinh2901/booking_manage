# Contexts

Shared React context providers belong here.

Keep this folder for cross-cutting client state such as authentication,
theme, locale, cart, or app-level preferences. Page-specific state should stay
near the page or feature component that owns it.
